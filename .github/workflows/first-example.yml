name: My First Yml flow.

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout repo  
          uses: actions/checkout@v7
        
        - name: list the files
          run: ls

        - name: Read file
          run: cat README.md