name: My First Yml flow.

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Welcome message
          run: echo "My first gihub action job"
        
        - name: list the files
          run: ls

        - name: Read file
          run: cat README.md